# Next Actions

## Immediate (next 48 hours)

1. Add malformed-binary corpus tests for parser/verifier hardening.
2. Add signature verification draft (public/private key) on top of checksum.
3. Add instruction-level trace filtering (`--trace-limit`).
4. Add workflow report script to compare outputs over repeated runs.
5. Add ecosystem onboarding docs (integration examples for external agent teams).

## Quality Gate Command

```bash
./scripts/quality_gate.sh
```
